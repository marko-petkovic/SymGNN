optimizer:
  #  These are all default parameters for the Adam optimizer
  lr: 0.0003
  betas: [ 0.9, 0.999 ]
  eps: 1e-08
  weight_decay: 0

lr_scheduler:
  mode: 'min'
  factor: 0.6
  patience: 10
  threshold: 0.0001