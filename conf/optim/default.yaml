optimizer:
  #  These are all default parameters for the Adam optimizer
  lr: 0.001
  betas: [ 0.9, 0.999 ]
  eps: 1e-08
  weight_decay: 0.01

lr_scheduler:
  step_size: 30
  gamma: 0.6

loading_weight:
  warmup_epochs: 100
  total_epochs: 125
  max_weight: 1.