expname: symgnn
workdir: experiments

core:
  version: 0.0.1
  tags:
    - ${now:%Y-%m-%d}


epochs: 400
device: 'cuda'

defaults:
  - dataset: gen
  - model: symgnn
  - logging: default
  - optim: default
  - isotherm: default
